package unitFiveMethod

object MethodScopeTest {
    
   def main(stringArray:Array[String]):Unit={
       var methodScope = new MethodScope;
       //仅当前类作用
       //methodScope.privateMethod;
   }
}